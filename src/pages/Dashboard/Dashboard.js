import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = (props) => {
  return <div>this should be a protected route</div>;
};

Dashboard.propTypes = {};

export default Dashboard;
